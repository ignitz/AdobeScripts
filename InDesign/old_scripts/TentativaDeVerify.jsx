/*Script para aplicar Clear Transformation e Fit Content to Window em todas as imagens em EPS2015 Yuri Niitsuma <ignitzhjfk@gmail.com>*//*var doc = app.activeDocument;// clear find change text preferences before searchapp.findTextPreferences = app.changeTextPreferences = NothingEnum.NOTHING;// this is scripting equivalent of the 'Find What:' edit text fieldapp.findTextPreferences.findWhat = "text";// search it in the active document// you can also search it in almost everywhere: story, text frame, paragraph, line, word, etcvar finds = doc.findText();if (finds.length > 0) { // something has been found// for the 1st found item display the name of the page where the 1st text frame (there can be several threaded frames) containing it is locatedalert("Found " + finds.length + " items, the first of them is on page " + finds[0].parentTextFrames[0].parentPage.name);}else { // found nothingalert("Nothing has been found");}// clear find change text preferences after searchapp.findTextPreferences = app.changeTextPreferences = NothingEnum.NOTHING;*/var i;// var links = app.activeDocument.allGraphics;var links = app.selection[0].allGraphics;var confirmacao = confirm("Are you Yuri?");if (confirmacao){	for (i=0; i<links.length; i++)	{		// if ([i].imageTypeName == "EPS")		if(links[i].itemLink.name.substr(links[i].itemLink.name.lastIndexOf(".")) == ".eps"			||			links[i].itemLink.name.substr(links[i].itemLink.name.lastIndexOf(".")) == ".EPS")		{			if (((links[i].geometricBounds[2] - links[i].geometricBounds[0]) != (links[i].parent.geometricBounds[2] - links[i].parent.geometricBounds[0]))				||				(links[i].geometricBounds[3] - links[i].geometricBounds[1]) != (links[i].parent.geometricBounds[3] - links[i].parent.geometricBounds[1])				)			{				alert(links[i].itemLink.name + "\nAparenta estar linkado incorretamente");			}		}	}	}else{    // alert("Cancelado!");    // for (i=0; i<10; i++)    //  	alert(links[i]);    alert(links);}