alert("Auto Relink nos EPS Post Script/none do Mathtype!\nYuri Niitsuma <ignitzhjfk@gmail.com>");var x = confirm("Tem certeza que deseja executar este script?"),	checkOrdem = true;if(x){	var doc = app.selection[0],      links = doc.allGraphics, count = 1;  	var myFolder = app.activeDocument.filePath,		ext, other;	for (i=0; i<links.length-1; i++)	{		ext = links[i].itemLink.name.substr(links[i].itemLink.name.lastIndexOf("e"));		other = links[i+1].itemLink.name.substr(links[i+1].itemLink.name.lastIndexOf("e"));		if (ext > other)			checkOrdem = false;	}	if(true)	{		for(i = 0; i < links.length; i++)  		{			if(links[i].itemLink.name.substr(links[i].itemLink.name.lastIndexOf(".")) == ".eps"				||				links[i].itemLink.name.substr(links[i].itemLink.name.lastIndexOf(".")) == ".EPS")			{				var old = File(links[i].itemLink.filePath),			        num = (count++).toString(),			        temp;			    var newname = myFolder + "/TMP/Eqn" + (count - 1).toString() + ".eps";			    old.rename(newname);			    links[i].itemLink.relink(File(newname));			}    		}	}	if(true)		alert("Concluido!");	else		alert("Cancelado, imagens estão em fora de ordem. Utilize o método do Excel!");}else{	alert("Script cancelado!");}